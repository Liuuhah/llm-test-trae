"""
测试 5W 提取在压缩前的触发逻辑
"""

print("=" * 80)
print("5W 提取触发顺序修复验证")
print("=" * 80)

print("\n📋 问题分析：")
print("""
原代码流程（有问题）：
1. 用户输入
2. 检查是否需要压缩 → 如果达到条件，先压缩
3. 发送请求给 LLM
4. 收到回复
5. 检查是否需要 5W 提取 ← ❌ 此时 chat_history 已经被压缩，轮数计算错误

问题示例：
  - 第 5 轮对话时，chat_history 有 10 条消息（5 轮）
  - 但压缩条件是 > 5 轮，所以第 6 轮才压缩
  - 第 6 轮时，chat_history 有 12 条消息（6 轮）
  - 触发压缩后，只剩下 5 条消息（2.5 轮）
  - rounds = 5 // 2 = 2，不满足 rounds % 5 == 0
  - 5W 提取永远不会触发！
""")

print("\n✅ 修复方案：")
print("""
新代码流程（正确）：
1. 用户输入
2. 检查是否需要 5W 提取 ← ✅ 在压缩之前执行
3. 检查是否需要压缩 → 如果需要，再压缩
4. 发送请求给 LLM
5. 收到回复

优点：
  ✅ 5W 提取在压缩之前执行，轮数计算准确
  ✅ 不会因为压缩导致错过提取时机
  ✅ 避免了重复调用
""")

print("\n🔧 修改内容：")
print("""
文件：practice04/chat_compress_client.py

修改 1：send_request_stream() 方法（第 551-560 行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

修改前：
```python
def send_request_stream(self, prompt, max_tokens=4096, debug=None):
    # 优先使用类的 debug_mode，如果参数为 None 则使用默认值
    if debug is None:
        debug = self.debug_mode
        
    # 在发送新请求前，检查是否需要压缩
    if self._should_compress():
        self._compress_chat_history()
```

修改后：
```python
def send_request_stream(self, prompt, max_tokens=4096, debug=None):
    # 优先使用类的 debug_mode，如果参数为 None 则使用默认值
    if debug is None:
        debug = self.debug_mode
        
    # 在发送新请求前，先检查是否需要提取 5W 信息（在压缩之前）
    self._check_and_extract_key_info()
        
    # 然后检查是否需要压缩
    if self._should_compress():
        self._compress_chat_history()
```

修改 2：主循环中的调用（第 1620-1621 行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

修改前：
```python
if response:
    self.add_to_history('assistant', response)
    print(f"[耗时: {time_taken:.2f}秒]")
    
    # 检查是否需要提取关键信息
    self._check_and_extract_key_info()
```

修改后：
```python
if response:
    self.add_to_history('assistant', response)
    print(f"[耗时: {time_taken:.2f}秒]")
    
    # 注意：5W 提取已经在 send_request_stream 开始时执行过了（压缩之前）
    # 这里不再重复检查，避免因为压缩导致轮数计算错误
```
""")

print("\n🧪 测试场景：")
print("""
场景 1：正常对话（无压缩）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

对话轮次：
  第 1-4 轮：正常对话
  第 5 轮：触发 5W 提取 ✅
  
期望输出：
  [关键信息提取] 检测到第5轮对话，开始提取关键信息...
  [关键信息提取] 正在调用 LLM...
  [关键信息提取] 提取成功:
  - Who: ...
  - What: ...
  - When: ...
  - Where: ...
  - Why: ...
  [关键信息提取] 📝 成功保存到: D:\\chat-log\\log.txt
  [关键信息提取] ✅ 完成

验证点：
  ✅ 在第 5 轮触发
  ✅ 保存到日志文件
  ✅ 日志文件中有新记录

""")

print("""
场景 2：对话超过 5 轮（触发压缩）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

对话轮次：
  第 1-4 轮：正常对话
  第 5 轮：触发 5W 提取 ✅（在压缩之前）
  第 6 轮：触发压缩（因为 > 5 轮）
  
期望输出（第 5 轮）：
  [关键信息提取] 检测到第5轮对话，开始提取关键信息...
  [关键信息提取] 正在调用 LLM...
  [关键信息提取] 提取成功:
  ...
  [关键信息提取] ✅ 完成

期望输出（第 6 轮）：
  [压缩触发] 对话轮数(6)超过限制(5)
  [压缩开始] 总共13条消息，压缩前9条，保留后4条
  [压缩中] 正在调用LLM总结历史对话...
  [压缩完成] 从13条消息压缩为5条消息

验证点：
  ✅ 第 5 轮先提取 5W
  ✅ 第 6 轮再压缩
  ✅ 顺序正确
  ✅ 日志文件中有第 5 轮的记录

""")

print("""
场景 3：连续对话（多次提取）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

对话轮次：
  第 5 轮：触发 5W 提取 ✅
  第 10 轮：触发 5W 提取 ✅
  第 15 轮：触发 5W 提取 ✅
  
期望输出：
  每次到达 5 的倍数轮次时，都会触发 5W 提取
  
验证点：
  ✅ 每 5 轮触发一次
  ✅ 不受压缩影响
  ✅ 日志文件中有多条记录

""")

print("\n📊 验证步骤：")
print("""
1. 启动聊天客户端
   python practice04/chat_compress_client.py

2. 进行 5 轮对话
   例如：
   - 第 1 轮：你好
   - 第 2 轮：我姓杨
   - 第 3 轮：我是女的
   - 第 4 轮：今天成都天气怎么样？
   - 第 5 轮：今天是几号？

3. 观察输出
   应该看到：
   [关键信息提取] 检测到第5轮对话，开始提取关键信息...
   [关键信息提取] 正在调用 LLM...
   [关键信息提取] 提取成功:
   ...

4. 检查日志文件
   打开 D:\\chat-log\\log.txt
   应该看到最新的记录（时间应该是今天）

5. 继续对话到第 6 轮
   应该看到压缩触发，但不会再触发 5W 提取
   
6. 继续对话到第 10 轮
   应该再次触发 5W 提取
""")

print("\n✅ 预期效果：")
print("""
修复后的行为：
  ✅ 5W 提取在压缩之前执行
  ✅ 轮数计算准确，不会错过触发时机
  ✅ 每 5 轮对话自动提取并保存
  ✅ 日志文件持续更新
  ✅ 不受压缩影响

与原需求的对比：
  ✅ 恢复了原来的保存逻辑
  ✅ 确保 5W 能正常保存到 D:\\chat-log\\log.txt
  ✅ 总结质量可以后续优化
""")

print("\n" + "=" * 80)
print("🎯 现在可以测试了！")
print("=" * 80)
print("""
请运行聊天客户端，进行至少 5 轮对话，然后检查：
1. 终端是否输出 5W 提取的日志
2. D:\\chat-log\\log.txt 是否有新记录
3. 记录的时间是否是今天

如果一切正常，说明修复成功！✅
""")
