# 5W 提取失败处理优化需求文档

## 📋 项目背景

我正在开发一个基于本地 LLM（Qwen3.5-9B）的聊天客户端，支持自动提取 5W（Who/What/When/Where/Why）关键信息并保存到日志文件。

**当前问题：**
当 5W 提取失败时，系统只输出"❌ 提取失败"，没有说明具体原因，用户不知道是：
- 对话内容信息太少，无法提取？
- Token 不足或上下文太长导致的技术故障？
- 其他原因？

**需求：**
优化 5W 提取失败处理，区分不同的失败场景，输出详细的原因说明。

---

## 🔴 当前问题分析

### 当前代码逻辑（chat_compress_client.py）

**文件位置：** `practice04/chat_compress_client.py`

**关键方法：**
1. `_check_and_extract_key_info()` - 第 1035 行
   - 检查是否达到提取条件（每 5 轮对话）
   - 调用 `_extract_5w_info()`

2. `_extract_5w_info()` - 第 1056 行
   - 获取最近对话历史
   - 构建提示词
   - 调用 LLM
   - 解析响应
   - **问题：失败时只打印 "❌ 提取失败"（第 1088 行）**

3. `_parse_extraction_response()` - 第 1172 行
   - 检查响应是否有效
   - 验证是否包含 5W 关键字段
   - **问题：不符合格式时只返回 None，没有说明原因**

**当前输出示例：**
```
[关键信息提取] 正在调用 LLM...
[关键信息提取] ❌ 提取失败
```

**问题：**
- ❌ 用户不知道失败原因
- ❌ 无法区分信息不足和技术故障
- ❌ 没有提供解决建议
- ❌ 失败记录没有保存到日志（无法追踪）

---

## 🎯 需求：区分两种失败场景

### 场景 1：信息不足（对话内容太少）

**触发条件：**
- 对话只是问候、闲聊
- 5W 字段中 4 个以上都是"未提及"
- LLM 返回内容过短（< 50 字符）
- 对话缺乏实质性内容（讨论、计划、事件等）

**示例对话：**
```
用户：你好
AI：你好！有什么可以帮你的吗？
用户：今天天气怎么样？
AI：请问您想查询哪个城市？
用户：算了，没什么
AI：好的，如果还有其他问题随时告诉我。
用户：嗯
AI：😊
用户：好的
AI：有什么我可以帮您的吗？
用户：没有了
```

**期望输出：**
```
[关键信息提取] ⚠️ 不满足提取条件：对话内容可提取的信息太少，没有值得提取的关键信息
```

**日志保存（可选但推荐）：**
```
============================================================
【记录时间】2026-04-24 11:00:00
【对话轮次】第 5 轮
【提取状态】❌ 无法提取
【失败原因】对话内容可提取的信息太少，没有值得提取的关键信息
【对话摘要】用户与 AI 进行了简短的问候和闲聊，没有实质性讨论内容。
============================================================
```

---

### 场景 2：技术故障（Token 不足/上下文太长/API 错误）

**触发条件：**
- LLM 返回空响应
- API 调用超时
- 网络错误
- 模型负载过高
- Token 限制导致无法处理

**期望输出：**
```
[关键信息提取] ❌ 提取失败（技术原因）：LLM 返回空响应，可能是 token 不足或模型错误
[关键信息提取] 建议：请检查网络连接或稍后重试
```

**日志保存：**
- ❌ **不保存**（临时性技术问题，重试后可能成功）

---

## 💡 解决方案：方案 A + B 结合

### 方案 A：代码层面验证（必须）

修改 `_parse_extraction_response()` 方法，返回三元组：`(content, status, reason)`

**判断逻辑：**

```python
def _parse_extraction_response(self, response_data):
    """解析 LLM 返回的提取结果
    
    Returns:
        tuple: (extracted_info, status, reason)
            - extracted_info: 提取的 5W 信息（成功时）或 None（失败时）
            - status: "success" | "insufficient_info" | "technical_error" | "format_error"
            - reason: 失败原因说明（成功时为 None）
    """
    
    # 1. 检查响应是否为空（技术故障）
    if response_data is None:
        return None, "technical_error", "API 调用失败，请检查网络连接"
    
    if 'choices' not in response_data or len(response_data['choices']) == 0:
        return None, "technical_error", "LLM 返回空响应，可能是 token 不足或模型错误"
    
    # 2. 提取内容
    choice = response_data['choices'][0]
    message = choice.get('message', {})
    content = message.get('content', '').strip()
    
    # 如果 content 为空，尝试从 reasoning_content 提取
    if not content:
        reasoning_content = message.get('reasoning_content', '')
        if reasoning_content:
            who_match = re.search(r'-\s*Who:', reasoning_content)
            if who_match:
                content = reasoning_content[who_match.start():].strip()
            else:
                lines = reasoning_content.strip().split('\n')
                content = '\n'.join(lines[-5:]).strip()
    
    # 如果仍然为空，技术故障
    if not content:
        return None, "technical_error", "LLM 返回空内容，无法提取信息"
    
    # 3. 检查 LLM 是否标记为"无法提取"（方案 B 的配合）
    if "【无法提取】" in content:
        # 提取原因
        reason_match = re.search(r'【无法提取】(.+)', content)
        reason = reason_match.group(1).strip() if reason_match else "对话内容无法提取"
        return None, "insufficient_info", reason
    
    # 4. 验证是否包含 5W 关键字段
    required_keywords = ['Who', 'What']
    has_required = any(keyword in content for keyword in required_keywords)
    
    if not has_required:
        # 检查是否内容过短（信息不足）
        if len(content) < 50:
            return None, "insufficient_info", "返回内容过短，无法提取有效信息"
        else:
            return None, "format_error", "返回内容不符合 5W 格式要求"
    
    # 5. 检查"未提及"数量（信息不足的二次验证）
    unmentioned_count = content.count("未提及")
    if unmentioned_count >= 4:  # 5 个字段中有 4 个以上是"未提及"
        return None, "insufficient_info", "对话内容可提取的信息太少，没有值得提取的关键信息"
    
    # 6. 提取成功
    return content, "success", None
```

---

### 方案 B：优化 Prompt 让 LLM 自我判断（推荐）

修改 `_build_extract_prompt()` 方法，添加自我判断规则。

**当前 Prompt（第 1090-1133 行）：**
```python
prompt = f"""请分析以下对话内容，按照 5W 规则提取关键信息。

【对话内容】（共{len(recent_messages)}条消息）
{conversation_text}

【提取要求】
请严格按照以下格式输出，每个字段一行：
- Who: [参与者姓名或角色，如果没有明确提及则写"未提及"]
- What: [发生的主要事件或讨论的核心话题，用一句话概括]
- When: [具体时间或时间段，如果没有则写"未提及"]
- Where: [地点或场所，如果没有则写"未提及"]
- Why: [原因、目的或动机，如果没有则写"未提及"]

【重要注意事项】
1. 必须分析所有对话内容，不能遗漏任何一条消息
2. 只提取对话中明确提到的信息，不要推测
3. 保持简洁，每个字段不超过 30 字
4. 如果某个信息在对话中出现多次，提取最重要的那个
5. 使用中文输出
6. 直接输出 5W 结果，不要输出任何思考过程、分析步骤或额外说明
7. 不要在输出中包含 "Thinking Process"、"分析"、"首先"、"Let" 等字样
8. 输出格式必须完全按照下面的示例，不要添加任何 Markdown 格式

【输出示例】
- Who: 张三
- What: 用户进行自我介绍
- When: 未提及
- Where: 北京
- Why: 寻求助手协助

【开始提取】"""
```

**修改后的 Prompt：**
```python
prompt = f"""请分析以下对话内容，按照 5W 规则提取关键信息。

【对话内容】（共{len(recent_messages)}条消息）
{conversation_text}

【提取规则】
请按照以下优先级处理：

1. **判断是否可提取**
   - 如果对话包含足够的实质性信息（讨论、计划、事件、决策等），请提取 5W
   - 如果对话只是问候、闲聊、或信息太少，无法提取有效信息，请输出：
     【无法提取】对话内容可提取的信息太少，没有值得提取的关键信息
   - 如果对话内容过长或超出你的处理能力，请输出：
     【无法提取】上下文太长，无法完整分析

2. **如果可以提取，请严格按照以下格式输出**
   - Who: [参与者姓名或角色，如果没有明确提及则写"未提及"]
   - What: [发生的主要事件或讨论的核心话题，用一句话概括]
   - When: [具体时间或时间段，如果没有则写"未提及"]
   - Where: [地点或场所，如果没有则写"未提及"]
   - Why: [原因、目的或动机，如果没有则写"未提及"]

【重要注意事项】
1. 必须分析所有对话内容，不能遗漏任何一条消息
2. 只提取对话中明确提到的信息，不要推测
3. 保持简洁，每个字段不超过 30 字
4. 如果某个信息在对话中出现多次，提取最重要的那个
5. 使用中文输出
6. 直接输出结果，不要输出任何思考过程、分析步骤或额外说明
7. 不要在输出中包含 "Thinking Process"、"分析"、"首先"、"Let" 等字样
8. 输出格式必须完全按照下面的示例，不要添加任何 Markdown 格式

【输出示例 - 可提取】
- Who: 张三
- What: 用户进行自我介绍
- When: 未提及
- Where: 北京
- Why: 寻求助手协助

【输出示例 - 无法提取】
【无法提取】对话内容可提取的信息太少，没有值得提取的关键信息

【开始提取】"""
```

**关键变化：**
- ✅ 添加了"判断是否可提取"的优先级规则
- ✅ 提供了两种输出示例（可提取 vs 无法提取）
- ✅ 明确告诉 LLM 何时输出"【无法提取】"

---

### 方案 C：修改 _extract_5w_info() 处理不同失败类型

**当前代码（第 1056-1088 行）：**
```python
def _extract_5w_info(self):
    """执行 5W 关键信息提取"""
    
    recent_messages = self.chat_history[-(self.extract_interval * 2):]
    extract_prompt = self._build_extract_prompt(recent_messages)
    
    print("[关键信息提取] 正在调用 LLM...")
    response_data = self._send_extract_request(
        extract_prompt, 
        max_tokens=512,
        temperature=0.5
    )
    
    extracted_info = self._parse_extraction_response(response_data)
    
    if extracted_info:
        print("[关键信息提取] 提取成功:")
        print(extracted_info)
        success = self._save_to_log_file(extracted_info, len(self.chat_history) // 2)
        if success:
            print("[关键信息提取] ✅ 完成")
        else:
            print("[关键信息提取] ⚠️ 保存失败")
    else:
        print("[关键信息提取] ❌ 提取失败")  # ❌ 只输出这一句
```

**修改后的代码：**
```python
def _extract_5w_info(self):
    """执行 5W 关键信息提取"""
    
    # 获取最近的对话历史
    recent_messages = self.chat_history[-(self.extract_interval * 2):]
    
    # 构建提取提示词
    extract_prompt = self._build_extract_prompt(recent_messages)
    
    # 调用 LLM 进行提取
    print("[关键信息提取] 正在调用 LLM...")
    response_data = self._send_extract_request(
        extract_prompt, 
        max_tokens=512,
        temperature=0.5
    )
    
    # 解析响应（返回三元组）
    extracted_info, status, reason = self._parse_extraction_response(response_data)
    
    # 根据状态处理
    if status == "success":
        print("[关键信息提取] 提取成功:")
        print(extracted_info)
        
        # 保存到文件
        success = self._save_to_log_file(extracted_info, len(self.chat_history) // 2)
        if success:
            print("[关键信息提取] ✅ 完成")
        else:
            print("[关键信息提取] ⚠️ 保存失败")
    
    elif status == "insufficient_info":
        # 场景 1：信息不足
        print(f"[关键信息提取] ⚠️ 不满足提取条件：{reason}")
        
        # 可选：保存失败记录到日志（便于追踪）
        self._save_failed_extraction(
            round_number=len(self.chat_history) // 2,
            reason=reason,
            recent_messages=recent_messages
        )
    
    elif status == "technical_error":
        # 场景 2：技术故障
        print(f"[关键信息提取] ❌ 提取失败（技术原因）：{reason}")
        print("[关键信息提取] 建议：请检查网络连接或稍后重试")
    
    else:
        # 其他错误（格式错误等）
        print(f"[关键信息提取] ❌ 提取失败：{reason}")
```

---

### 方案 D：添加 _save_failed_extraction() 方法（可选但推荐）

**新增方法：**
```python
def _save_failed_extraction(self, round_number, reason, recent_messages):
    """保存无法提取的记录到日志（便于追踪和分析）
    
    Args:
        round_number: 对话轮次
        reason: 失败原因
        recent_messages: 最近的对话消息
    """
    
    try:
        # 生成对话摘要
        summary = self._generate_dialogue_summary(recent_messages)
        
        # 获取当前时间戳
        timestamp = time.strftime("%Y-%m-%d %H:%M:%S")
        
        # 格式化日志条目
        log_entry = f"\n{'='*60}\n"
        log_entry += f"【记录时间】{timestamp}\n"
        log_entry += f"【对话轮次】第 {round_number} 轮\n"
        log_entry += f"【提取状态】❌ 无法提取\n"
        log_entry += f"【失败原因】{reason}\n"
        log_entry += f"【对话摘要】{summary}\n"
        log_entry += f"{'='*60}\n"
        
        # 追加写入文件
        with open(self.log_file_path, 'a', encoding='utf-8') as f:
            f.write(log_entry)
        
        print(f"[关键信息提取] 📝 已记录失败信息到日志")
        
    except Exception as e:
        print(f"[关键信息提取] ⚠️ 保存失败记录时出错: {str(e)}")

def _generate_dialogue_summary(self, recent_messages):
    """生成对话摘要（用于失败记录）
    
    Args:
        recent_messages: 最近的对话消息列表
        
    Returns:
        str: 对话摘要（50 字以内）
    """
    try:
        # 提取用户和 AI 的消息
        user_msgs = [msg.get('content', '') for msg in recent_messages if msg.get('role') == 'user']
        ai_msgs = [msg.get('content', '') for msg in recent_messages if msg.get('role') == 'assistant']
        
        # 拼接摘要
        summary_parts = []
        if user_msgs:
            summary_parts.append(f"用户：{'、'.join(user_msgs[:3])}")
        if ai_msgs:
            summary_parts.append(f"AI：{'、'.join(ai_msgs[:3])}")
        
        summary = ' | '.join(summary_parts)
        
        # 截断到 50 字
        if len(summary) > 50:
            summary = summary[:47] + "..."
        
        return summary
        
    except Exception as e:
        return f"生成摘要失败: {str(e)}"
```

---

## 📊 测试验证

### 测试用例 1：信息不足的对话

**输入对话（5 轮）：**
```
用户：你好
AI：你好！有什么可以帮你的吗？
用户：今天天气怎么样？
AI：请问您想查询哪个城市？
用户：算了，没什么
AI：好的，如果还有其他问题随时告诉我。
用户：嗯
AI：😊
用户：好的
AI：有什么我可以帮您的吗？
```

**期望输出：**
```
[关键信息提取] 正在调用 LLM...
[关键信息提取] ⚠️ 不满足提取条件：对话内容可提取的信息太少，没有值得提取的关键信息
[关键信息提取] 📝 已记录失败信息到日志
```

**日志内容：**
```
============================================================
【记录时间】2026-04-24 11:00:00
【对话轮次】第 5 轮
【提取状态】❌ 无法提取
【失败原因】对话内容可提取的信息太少，没有值得提取的关键信息
【对话摘要】用户：你好、今天天气怎么样？、算了，没什么 | AI：你好！有什么可以帮你的...
============================================================
```

**验证标准：**
- ✅ 输出明确的原因说明
- ✅ 识别出信息不足
- ✅ 保存失败记录到日志
- ✅ 不保存无效的 5W 数据

---

### 测试用例 2：有实质内容的对话

**输入对话（5 轮）：**
```
用户：我想学习编程
AI：很好！你想学什么语言？
用户：Python 吧
AI：Python 是个好选择
用户：有什么推荐的学习资源吗？
AI：推荐官方文档和 Codecademy
用户：好的谢谢
AI：不客气
用户：那我开始学了
AI：加油！
```

**期望输出：**
```
[关键信息提取] 正在调用 LLM...
[关键信息提取] 提取成功:
- Who: 用户（学习者）
- What: 计划学习 Python 编程
- When: 未提及
- Where: 未提及
- Why: 个人兴趣/技能提升
[关键信息提取] 📝 成功保存到: D:\chat-log\log.txt
[关键信息提取] ✅ 完成
```

**验证标准：**
- ✅ 正常提取 5W 信息
- ✅ "未提及"的字段正确标注
- ✅ 保存到日志文件

---

### 测试用例 3：技术故障模拟

**模拟场景：**
- API 超时
- 模型返回空响应
- 网络错误

**期望输出：**
```
[关键信息提取] 正在调用 LLM...
[关键信息提取] ❌ 提取失败（技术原因）：LLM 返回空响应，可能是 token 不足或模型错误
[关键信息提取] 建议：请检查网络连接或稍后重试
```

**验证标准：**
- ✅ 区分技术故障和信息不足
- ✅ 给出具体错误信息
- ✅ 提供解决建议
- ✅ 不保存失败记录到日志

---

## 🔧 实施步骤

### 优先级

**P0（必须修改）：**
1. 修改 `_parse_extraction_response()` - 返回三元组，添加失败分类
2. 修改 `_extract_5w_info()` - 处理不同失败类型

**P1（强烈建议）：**
3. 优化 `_build_extract_prompt()` - 让 LLM 自我判断

**P2（可选但推荐）：**
4. 添加 `_save_failed_extraction()` - 保存失败记录
5. 添加 `_generate_dialogue_summary()` - 生成对话摘要

---

### 修改清单

**文件：** `practice04/chat_compress_client.py`

**修改位置 1：** 第 1172-1225 行
- 方法：`_parse_extraction_response()`
- 修改：返回三元组 `(content, status, reason)`，添加失败分类逻辑

**修改位置 2：** 第 1056-1088 行
- 方法：`_extract_5w_info()`
- 修改：处理不同的失败类型，输出详细原因

**修改位置 3：** 第 1090-1133 行
- 方法：`_build_extract_prompt()`
- 修改：添加自我判断规则，提供两种输出示例

**新增位置：** 在 `_save_to_log_file()` 方法后面（约第 1267 行）
- 新增方法：`_save_failed_extraction()`
- 新增方法：`_generate_dialogue_summary()`

---

## ⚠️ 注意事项

1. **保持向后兼容**
   - 不影响现有的成功提取逻辑
   - 只优化失败处理
   - 确保现有日志格式不变

2. **错误处理**
   - 所有新增代码都要有 try-except
   - 失败记录保存出错不应影响主流程

3. **日志文件大小**
   - 失败记录应该简洁（对话摘要 50 字以内）
   - 避免日志文件过大

4. **性能影响**
   - 新增的逻辑不应显著增加处理时间
   - 生成摘要要高效

---

## ✅ 验收标准

修改完成后，需要满足以下条件：

1. **功能正确性**
   - [ ] 能正确区分信息不足和技术故障
   - [ ] 输出详细的原因说明
   - [ ] LLM 能根据 Prompt 自我判断

2. **用户体验**
   - [ ] 用户清楚知道为什么无法提取
   - [ ] 提供明确的解决建议（技术故障时）
   - [ ] 输出格式清晰易读

3. **数据完整性**
   - [ ] 成功的 5W 提取正常保存到日志
   - [ ] 失败的提取（信息不足）标记清楚
   - [ ] 技术故障不保存失败记录

4. **代码质量**
   - [ ] 不引入新的 bug
   - [ ] 保持代码可读性
   - [ ] 添加必要的注释
   - [ ] 所有新增方法都有错误处理

---

## 📚 参考资料

**相关文件：**
- 主客户端：`practice04/chat_compress_client.py`
- 测试脚本：`practice04/test_scripts/test_5w_extraction_failure.py`

**关键代码位置：**
- `_parse_extraction_response()` - 第 1172-1225 行
- `_extract_5w_info()` - 第 1056-1088 行
- `_build_extract_prompt()` - 第 1090-1133 行

---

## 📧 实施建议

1. **先做 P0 修改**（返回三元组 + 处理失败类型）
2. **测试验证** P0 修改的效果
3. **再做 P1 修改**（优化 Prompt）
4. **最后做 P2 修改**（保存失败记录）

每完成一步都要测试验证，确保没有问题再继续下一步。

---

**文档版本：** 1.0  
**创建日期：** 2026-04-24  
**最后更新：** 2026-04-24
