# 日志格式优化需求文档

## 📋 任务概述

**只修改日志保存逻辑**，让保存到 `D:\chat-log\log.txt` 的内容更加详细和结构化。

**不修改任何其他功能**：
- ❌ 不修改提取逻辑
- ❌ 不修改压缩逻辑
- ❌ 不修改计数器逻辑
- ❌ 不修改 Prompt
- ✅ **只修改 `_save_to_log_file()` 方法**

---

## 🎯 目标日志格式

### **成功提取的日志格式：**

```
============================================================
【记录时间】2026-04-25 13:21:23
【对话轮次】第 9 轮
【提取模式】标准模式
【计数器状态】extract_message_counter = 5
【触发原因】计数器达到阈值 (5 % 5 == 0)
============================================================

📊 5W 提取结果：
Who: 刘先生
What: 用户计划明日在成都进行篮球训练
When: 2026年4月26日（明天）
Where: 成都东软
Why: 准备进行中锋位置的篮球训练

📝 原始对话（最近 10 条消息）：
[1] user: 我要知道成都明天的天气
[2] assistant: 📅 **成都明天（2026-04-26）天气预报**...
[3] user: 我来子成都东软我姓刘
[4] assistant: 你好，刘先生！👋 ...
[5] user: 我爱打篮球的我是中锋欸
[6] assistant: 哇，刘先生！🏀 您是中锋啊...
[7] user: 我明天要去打篮球了，你同意吗
[8] assistant: 当然同意啊！👍 ...
[9] user: 明天多少号呀
[10] assistant: 根据刚才查询的成都天气预报数据...

✅ 提取状态：成功
💾 保存位置：D:\chat-log\log.txt
🗜️ 后续操作：触发压缩（保留后 6 条）
============================================================
```

---

### **失败提取的日志格式：**

```
============================================================
【记录时间】2026-04-25 13:09:35
【对话轮次】第 5 轮
【提取模式】标准模式
【计数器状态】extract_message_counter = 5
【触发原因】计数器达到阈值 (5 % 5 == 0)
============================================================

❌ 提取状态：失败
【失败原因】对话内容可提取的信息太少，没有值得提取的关键信息

📝 原始对话（最近 10 条消息）：
[1] user: 你好
[2] assistant: 你好！有什么我可以帮助你的吗？...
[3] user: 你好
[4] assistant: 你好！😊 我随时准备帮助你...
[5] user: 你好
[6] assistant: 你好！👋 很高兴见到你...
[7] user: 你好
[8] assistant: 你好！👋 我是你的AI助手...
[9] user: 你好
[10] assistant: 你好！👋 很高兴见到你...

💾 保存位置：D:\chat-log\log.txt
🗜️ 后续操作：未触发压缩
============================================================
```

---

### **累积模式的日志格式：**

```
============================================================
【记录时间】2026-04-25 11:17:30
【对话轮次】第 6 轮
【提取模式】累积模式（包含历史对话）
【计数器状态】extract_message_counter = 10
【触发原因】计数器达到阈值 (10 % 5 == 0)，且之前有失败记录
============================================================

📊 5W 提取结果：
Who: 成都东软的学生与AI助手
What: 查询成都明日天气并咨询是否适合打篮球
When: 2026年4月26日（明天）
Where: 成都
Why: 计划进行打篮球活动

📝 原始对话（最近 20 条消息）：
[1] user: 你好
[2] assistant: 你好！👋 我是你的AI助手...
...
[11] user: 我要知道成都明天的天气
[12] assistant: 📅 **成都明天（2026-04-26）天气预报**...
...
[19] user: 我明天要去打篮球了，你同意吗
[20] assistant: 当然同意啊！👍 ...

✅ 提取状态：成功（累积模式）
💾 保存位置：D:\chat-log\log.txt
🗜️ 后续操作：触发压缩（保留后 6 条）
============================================================
```

---

## 🔧 具体实现方案

### **修改位置：** `_save_to_log_file()` 方法

**文件：** `f:/administrator/desktop/llm-test-trae/practice04/chat_compress_client.py`

**当前方法签名：**
```python
def _save_to_log_file(self, extracted_info, round_number):
    """将提取的关键信息保存到日志文件"""
```

**需要添加的参数：**
```python
def _save_to_log_file(self, extracted_info, round_number, recent_messages=None, is_cumulative=False, counter_value=0, should_compress=False):
    """将提取的关键信息保存到日志文件（增强版）"""
```

---

### **修改步骤：**

#### **步骤 1：更新方法签名**

**原代码：**
```python
def _save_to_log_file(self, extracted_info, round_number):
    """将提取的关键信息保存到日志文件"""
```

**修改为：**
```python
def _save_to_log_file(self, extracted_info, round_number, recent_messages=None, is_cumulative=False, counter_value=0, should_compress=False):
    """将提取的关键信息保存到日志文件（增强版）
    
    Args:
        extracted_info: 提取的 5W 信息字符串
        round_number: 对话轮次
        recent_messages: 最近的消息列表（用于记录原始对话）
        is_cumulative: 是否为累积模式
        counter_value: 当前计数器值
        should_compress: 是否触发了压缩
    """
```

---

#### **步骤 2：构建详细的日志内容**

**在方法开头添加：**

```python
# 获取当前时间
from datetime import datetime
current_time = datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# 判断提取模式
if is_cumulative:
    extract_mode = "累积模式（包含历史对话）"
else:
    extract_mode = "标准模式"

# 判断触发原因
trigger_reason = f"计数器达到阈值 ({counter_value} % {self.extract_interval} == 0)"
if is_cumulative and counter_value > self.extract_interval:
    trigger_reason += "，且之前有失败记录"

# 开始构建日志内容
log_content = "\n" + "=" * 60 + "\n"
log_content += f"【记录时间】{current_time}\n"
log_content += f"【对话轮次】第 {round_number} 轮\n"
log_content += f"【提取模式】{extract_mode}\n"
log_content += f"【计数器状态】extract_message_counter = {counter_value}\n"
log_content += f"【触发原因】{trigger_reason}\n"
log_content += "=" * 60 + "\n\n"
```

---

#### **步骤 3：添加 5W 提取结果或失败信息**

```python
# 判断是成功还是失败
if extracted_info and "无法提取" not in extracted_info:
    # 成功提取
    log_content += "📊 5W 提取结果：\n"
    log_content += extracted_info.strip() + "\n\n"
    log_content += "✅ 提取状态：成功"
    if is_cumulative:
        log_content += "（累积模式）"
    log_content += "\n"
else:
    # 提取失败
    log_content += "❌ 提取状态：失败\n"
    # 提取失败原因
    if extracted_info:
        reason_line = [line for line in extracted_info.split('\n') if '原因' in line or '无法提取' in line]
        if reason_line:
            log_content += f"【失败原因】{reason_line[0].strip()}\n"
        else:
            log_content += f"【失败原因】{extracted_info.strip()}\n"
    else:
        log_content += "【失败原因】未知错误\n"
    log_content += "\n"
```

---

#### **步骤 4：添加原始对话记录**

```python
# 添加原始对话记录
if recent_messages:
    log_content += f"\n📝 原始对话（最近 {len(recent_messages)} 条消息）：\n"
    for i, msg in enumerate(recent_messages, 1):
        role = msg.get('role', 'unknown')
        content = msg.get('content', '')
        
        # 对 assistant 的内容进行截断和总结
        if role == 'assistant':
            # 只保留前 50 个字符，后面加省略号
            if len(content) > 50:
                summary = content[:50].replace('\n', ' ').strip() + "..."
            else:
                summary = content.replace('\n', ' ').strip()
            log_content += f"[{i}] {role}: {summary}\n"
        else:
            # user 的消息完整显示
            log_content += f"[{i}] {role}: {content}\n"
    log_content += "\n"
```

---

#### **步骤 5：添加后续操作信息**

```python
# 添加保存位置和后续操作
log_content += f"💾 保存位置：{self.log_file_path}\n"

if should_compress:
    log_content += f"🗜️ 后续操作：触发压缩（保留后 {self.compress_keep_rounds * 2} 条）\n"
else:
    log_content += f"🗜️ 后续操作：未触发压缩\n"

log_content += "=" * 60 + "\n"
```

---

#### **步骤 6：写入文件**

**保持原有的文件写入逻辑不变：**

```python
# 写入文件（保持原有逻辑）
try:
    with open(self.log_file_path, 'a', encoding='utf-8') as f:
        f.write(log_content)
    print(f"[日志保存] 成功保存到: {self.log_file_path}")
    return True
except Exception as e:
    print(f"[日志保存] ❌ 保存失败: {e}")
    return False
```

---

### **步骤 7：更新调用处**

**位置 1：** `_extract_5w_info()` 方法中调用 `_save_to_log_file()` 的地方

**原代码：**
```python
success = self._save_to_log_file(extracted_info, len(self.chat_history) // 2)
```

**修改为：**
```python
# 检查是否需要压缩
should_compress = self._should_compress()

success = self._save_to_log_file(
    extracted_info=extracted_info,
    round_number=len(self.chat_history) // 2,
    recent_messages=recent_messages,
    is_cumulative=is_cumulative,
    counter_value=self.extract_message_counter,
    should_compress=should_compress
)
```

---

**位置 2：** `_save_failed_extraction()` 方法中调用 `_save_to_log_file()` 的地方（如果有）

**同样需要传递新参数：**

```python
success = self._save_to_log_file(
    extracted_info=f"【无法提取】{reason}",
    round_number=round_number,
    recent_messages=recent_messages,
    is_cumulative=(self.extract_message_counter > self.extract_interval),
    counter_value=self.extract_message_counter,
    should_compress=False  # 失败时不触发压缩
)
```

---

## ⚠️ 注意事项

1. **编码问题**
   - 确保文件以 UTF-8 编码保存
   - `open(self.log_file_path, 'a', encoding='utf-8')`

2. **Assistant 内容截断**
   - 只保留前 50 个字符
   - 去除换行符
   - 添加省略号

3. **User 内容完整显示**
   - 不截断用户消息
   - 保持原始格式

4. **不修改其他逻辑**
   - 只修改 `_save_to_log_file()` 方法
   - 只更新调用处的参数传递
   - 其他所有功能保持不变

5. **向后兼容**
   - 新参数都有默认值
   - 如果其他地方调用了这个方法，不会报错

---

## 🎯 验收标准

- [ ] 日志格式符合预期（成功、失败、累积模式）
- [ ] 包含计数器状态和触发原因
- [ ] 包含原始对话记录（assistant 内容截断）
- [ ] 包含后续操作信息
- [ ] 文件编码正确（UTF-8，无乱码）
- [ ] 不影响其他功能
- [ ] 测试通过

---

## 📂 相关文件

- **主文件：** `f:/administrator/desktop/llm-test-trae/practice04/chat_compress_client.py`
- **日志文件：** `D:\chat-log\log.txt`

---

## 💬 实施建议

1. **先备份当前代码**
2. **只修改 `_save_to_log_file()` 方法**
3. **更新调用处的参数传递**
4. **测试三种场景：**
   - 成功提取（标准模式）
   - 失败提取
   - 累积模式提取
5. **检查日志文件格式和内容**

祝开发顺利！🚀
